<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PREPTO - Career Guidance</title>
    <link rel="stylesheet" href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        /* Importing Google font - Poppins */
        @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap");

        /* Defining Custom Variables */
        :root {
            --primary-color: #3b141c;
            --secondary-color: #f3961c;
            --dark-color: #252525;
            --white-color: #fff;
            --light-gray-color: #f2f2f2;
            --site-max-width: 1300px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        body {
            background-color: var(--light-gray-color);
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        /* Navigation Sidebar */
        .nav {
            position: fixed;
            top: 0;
            left: 0;
            width: 250px;
            height: 100vh;
            background-color: var(--primary-color);
            color: var(--white-color);
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            box-shadow: 2px 0px 5px rgba(0, 0, 0, 0.2);
            z-index: 100; /* Ensure it's above other content */
        }

        .nav .nav_logo {
            margin-bottom: 20px;
        }

        .nav .nav_logo a {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .nav .nav_logo img {
            width: 40px;
            height: 40px;
            border-radius: 50%; /* Make it circular */
            object-fit: cover; /* Prevents image distortion */
        }

        .nav .nav_logo h2 {
            font-size: 24px;
            font-weight: 600;
            color: var(--white-color); /* Ensure the title is visible */
        }

        .nav ul {
            width: 100%;
        }

        .nav ul li a {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 15px;
            border-radius: 8px;
            transition: background-color 0.2s ease;
        }

        .nav ul li a:hover {
            background-color: var(--secondary-color);
            color: var(--primary-color);
        }

        .nav ul li a i {
            font-size: 20px;
            width: 24px; /* Ensure icons align properly */
            text-align: center;
        }

        .nav ul li a span {
            font-size: 16px;
        }

        /* Main Content Area */
        .main {
            margin-left: 250px; /* Account for the sidebar width */
            padding: 20px;
            min-height: 100vh; /* Ensure full height */
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Form Container */
        .form-container {
            background-color: var(--white-color);
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin: 20px;
            max-width: 800px;
            width: 100%;
        }

        .form-title {
            color: var(--primary-color);
            font-size: 28px;
            font-weight: 600;
            margin-bottom: 30px;
            text-align: center;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: var(--dark-color);
            font-weight: 500;
        }

        .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
            min-height: 150px;
            resize: vertical;
            font-family: inherit;
        }

        .form-group textarea:focus {
            outline: none;
            border-color: var(--secondary-color);
            box-shadow: 0 0 5px rgba(243, 150, 28, 0.2);
        }

        .submit-btn {
            background-color: var(--secondary-color);
            color: var(--primary-color);
            padding: 12px 30px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.2s ease;
            display: block;
            margin: 20px auto;
        }

        .submit-btn:hover {
            background-color: var(--primary-color);
            color: var(--white-color);
        }

        .help-text {
            color: #666;
            font-size: 14px;
            margin-top: 5px;
        }

        .logout {
            margin-top: auto;
            margin-bottom: 20px;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .nav {
                width: 100%;
                height: auto;
                position: static;
                flex-direction: row;
                justify-content: space-between;
                align-items: center;
                padding: 10px;
            }

            .nav .nav_logo {
                margin-bottom: 0;
            }

            .nav ul {
                display: flex;
                width: auto;
            }

            .nav ul li {
                margin: 0;
            }

            .nav ul li a {
                padding: 5px 10px;
            }

            .main {
                margin-left: 0;
                padding: 80px 20px 20px; /* Added top padding to account for fixed nav */
            }

            .form-container {
                margin: 10px;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation Sidebar -->
    <div class="nav">
        <div class="nav_logo">
            <a href="/dashboard">
                <img src="./images/WhatsApp Image 2025-03-01 at 07.23.51_0acb7dd0.jpg" alt="Prepto Logo">
                <h2>Prepto</h2>
            </a>
        </div>
        <ul>
            <li><a href="/analyze"><i class="fas fa-file-alt"></i><span>ATS Score</span></a></li>
            <li><a href="/resume-builder"><i class="fas fa-file-pdf"></i><span>Resume Builder</span></a></li>
            <li><a href="/certificates"><i class="fas fa-award"></i><span>Certificates</span></a></li>
            <li><a href="/progress"><i class="fas fa-chart-bar"></i><span>Progress</span></a></li>
            <li><a href="/mock-interviews"><i class="fas fa-microphone"></i><span>Mock Interviews</span></a></li>
            <li><a href="/mocktest"><i class="fas fa-pencil-alt"></i><span>Mock Tests</span></a></li>
            <li><a href="/profile"><i class="fas fa-user-circle"></i><span>Profile</span></a></li>
            <li><a href="/careerguidance"><i class="fas fa-graduation-cap"></i><span>Career Guidance</span></a></li>
            <li class="logout"><a href="/logout"><i class="fas fa-sign-out-alt"></i><span>Logout</span></a></li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main">
        <div class="form-container">
            <h1 class="form-title">Career Guidance</h1>
            <form action="/career-guidance" method="POST">
                <div class="form-group">
                    <label for="skills">Enter Your Skills</label>
                    <textarea 
                        id="skills" 
                        name="skills" 
                        placeholder="Enter your skills (e.g., JavaScript, Python, React, Problem Solving, Communication)"
                        required
                    ></textarea>
                    <p class="help-text">List your skills, separated by commas. Include both technical and soft skills.</p>
                </div>
                <button type="submit" class="submit-btn">
                    <i class="fas fa-search"></i> Get Career Guidance
                </button>
            </form>
        </div>
    </div>
</body>
</html>
